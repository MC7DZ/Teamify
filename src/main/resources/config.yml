# =======================================================
#                    TeamsPlugin Config
#      Every setting the plugin uses lives in here.
# =======================================================

general:
  # Prefix used in all plugin chat messages
  prefix: "&8✦ &b&lTeamify &8✦ &r"
  # Language file to use from /lang folder (en.yml, fr.yml, etc)
  language: "en"
  # Enable debug logging to console
  debug: false
  # Save data every X minutes (auto-save)
  autosave-interval-minutes: 5

team-creation:
  # Minimum / maximum characters for a team name
  min-name-length: 3
  max-name-length: 16
  # Minimum / maximum characters for a team tag (shown in chat/tab)
  min-tag-length: 2
  max-tag-length: 5
  # Allow color codes in team names
  allow-color-codes: true
  # Regex whitelist pattern for team names
  name-regex: "^[a-zA-Z0-9_]+$"
  # Cost to create a team (requires Vault economy, ignored if 0)
  creation-cost: 0.0
  # Cooldown in seconds before a player can create another team after leaving one
  creation-cooldown-seconds: 300
  # Block duplicate/case-insensitive team names
  block-duplicate-names: true

team-limits:
  # Maximum members per team (0 = unlimited)
  max-members: 20
  # Maximum number of team homes that can be set
  max-homes: 1
  # Maximum allies a team can have
  max-allies: 5
  # Maximum enemies a team can declare
  max-enemies: 10
  # Maximum length of the team description/MOTD
  max-description-length: 100
  # Maximum team level (used with the leveling/perks system below)
  max-level: 50

roles:
  # Role hierarchy from lowest to highest. Feel free to rename.
  hierarchy:
    - "MEMBER"
    - "MODERATOR"
    - "CO_LEADER"
    - "LEADER"
  # Permissions per role
  permissions:
    MEMBER:
      can-invite: false
      can-kick: false
      can-set-home: false
      can-edit-description: false
      can-manage-relations: false
      can-promote: false
      can-access-bank: false
      can-withdraw-bank: false
      can-toggle-pvp: false
      can-customize-team: false
    MODERATOR:
      can-invite: true
      can-kick: false
      can-set-home: false
      can-edit-description: false
      can-manage-relations: false
      can-promote: false
      can-access-bank: true
      can-withdraw-bank: false
      can-toggle-pvp: false
      can-customize-team: false
    CO_LEADER:
      can-invite: true
      can-kick: true
      can-set-home: true
      can-edit-description: true
      can-manage-relations: true
      can-promote: false
      can-access-bank: true
      can-withdraw-bank: true
      can-toggle-pvp: true
      can-customize-team: true
    LEADER:
      can-invite: true
      can-kick: true
      can-set-home: true
      can-edit-description: true
      can-manage-relations: true
      can-promote: true
      can-access-bank: true
      can-withdraw-bank: true
      can-toggle-pvp: true
      can-customize-team: true

invites:
  # How long an invite stays valid before expiring (seconds)
  expire-seconds: 60
  # Require confirmation click in GUI instead of just chat command
  require-gui-confirmation: true
  # Allow players to invite offline players (invite queued until they join)
  allow-offline-invites: true

home:
  # Master toggle for the /team home command. Default false (disabled).
  enable-home: false
  # Master toggle for the /team sethome command. Default false (disabled).
  enable-sethome: false
  # Teleport delay in seconds before /team home teleports (0 = instant)
  teleport-delay-seconds: 3
  # Cancel teleport if the player moves during the delay
  cancel-on-move: true
  # Cooldown between uses of /team home in seconds
  cooldown-seconds: 30
  # Cost of teleporting home (Vault economy, 0 = free)
  teleport-cost: 0.0

chat:
  # Enable team-only chat channel toggle (/team chat)
  enable-team-chat: true
  # Format for team chat messages
  team-chat-format: "&8[&bTeam&8] &7{role} &f{player}&8: &f{message}"
  # Format for team announcements (leader broadcasts)
  announcement-format: "&8[&6Announce&8] &e{team}&8: &f{message}"
  # Show a tag prefix like [TAG] before player names in global chat
  show-tag-in-global-chat: true
  global-chat-tag-format: "&7[&b{tag}&7] &f{player}&8: &f{message}"

relations:
  # Enable alliance system (enables/disables /team allyinvite, /team allyleave, /team allychat)
  enable-allies: true
  # Enable enemy/rivalry declarations
  enable-enemies: true
  # Require both teams to accept an alliance request
  mutual-alliance-required: true
  # Enable the /team allychat command (only checked if enable-allies is also true)
  enable-ally-chat: true
  # How long an ally invite stays valid before expiring (seconds)
  ally-invite-expire-seconds: 60
  # Format for ally chat messages
  ally-chat-format: "&8[&aAlly&8] &7{team} &7{role} &f{player}&8: &f{message}"
  # Friendly fire settings (PVP)
  friendly-fire:
    within-team: true
    between-allies: true
    between-enemies: true
    neutral-default: true

# This mirrors vanilla's own team visibility option - the same thing
# /minecraft:team modify <team> seeFriendlyInvisibles true does - but scoped
# to Teamify's teams/allies. When enabled, invisible teammates/allies become
# actually visible in-game to the rest of the team. This is a global,
# server-enforced setting - there is no per-team toggle.
visibility:
  see-members-when-invis:
    default: false
  see-allies-when-invis:
    default: false

bank:
  # Enable a shared team bank (requires Vault)
  enable-team-bank: false
  # Starting balance for new teams
  starting-balance: 0.0
  # Maximum balance a team bank can hold (0 = unlimited)
  max-balance: 1000000.0
  # Tax percentage taken on deposits (0 = none)
  deposit-tax-percent: 0.0

team-customization:
  # Lets teams pick a color (used for their name/tag in menus & messages)
  # and a custom item (their icon in the team list / relations menus),
  # both editable from /team settings. Requires the can-customize-team
  # role permission.
  enable-color: true
  enable-custom-item: true
  # The color a new team starts with
  default-color: "WHITE"
  # Colors players can cycle through in the settings menu
  available-colors:
    - "WHITE"
    - "RED"
    - "GOLD"
    - "YELLOW"
    - "GREEN"
    - "AQUA"
    - "BLUE"
    - "LIGHT_PURPLE"
    - "DARK_PURPLE"
    - "GRAY"
    - "BLACK"

leveling:
  # Enable team leveling / XP system
  enable-leveling: false
  # XP required for level 2 (scales upward using the multiplier below)
  base-xp-required: 1000
  # Multiplier applied per level (exponential growth)
  xp-multiplier: 1.35
  # XP granted for various actions
  xp-sources:
    mob-kill: 5
    player-kill: 25
    playtime-per-hour: 15
  # Perks unlocked at certain levels (purely config-driven, read by plugin)
  perks:
    5:
      extra-homes: 0
    10:
      extra-homes: 1
    25:
      extra-homes: 1
    50:
      extra-homes: 2

pvp:
  # Enable team-based PvP tracking (kills/deaths between teams)
  track-team-pvp-stats: true
  # Grace period after joining a team where a player can't be attacked (seconds)
  join-invulnerability-seconds: 10
  # Enable team elimination war-mode events
  enable-war-mode: false

disband:
  # Require confirmation (GUI or chat) before disbanding
  require-confirmation: true
  # Cooldown in seconds before the same player can create a new team after disbanding
  cooldown-after-disband-seconds: 60
  # Refund percentage of creation cost when disbanding
  refund-percent-on-disband: 50

transfer:
  # Require confirmation (GUI) before transferring team ownership
  require-confirmation: true

storage:
  # Storage backend: YAML, SQLITE, or MYSQL
  type: "YAML"
  mysql:
    host: "localhost"
    port: 3306
    database: "teamsplugin"
    username: "root"
    password: "password"
    use-ssl: false
    table-prefix: "teams_"

integrations:
  #those might not work they are experimantal and didnt touch them much
  vault:
    enabled: true
  placeholderapi:
    enabled: true
  worldguard:
    enabled: false
  discord-webhook:
    enabled: false
    url: ""
    notify-on-create: true
    notify-on-disband: true
    notify-on-war: true

gui:
  # Master toggle for the GUI system
  enabled: true
  # Sounds below use modern Minecraft sound-event keys (the same names you'd
  # type in /playsound, minus the "minecraft:" namespace) - not the old
  # Bukkit Sound enum names like "ENTITY_PLAYER_LEVELUP".
  # Sound played when opening any team GUI
  open-sound: "ui.button.click"
  # Sound played on a successful action (invite sent, promote, etc)
  success-sound: "entity.player.levelup"
  # Sound played on an error / denied action
  error-sound: "entity.villager.no"

  # NOTE: individual GUI items also support two optional per-item keys
  # wherever an item is built from a config section (see the "settings"
  # item below for an example):
  #   custom-model-data: 1001   # lets a resource pack swap the item's texture
  #   glow: true                # adds an enchant glint with no visible enchant
  # These aren't set on every item by default - add them to whichever items
  # you want a custom look for.

  main-menu:
    title: "&8&lTeam Menu"
    size: 27
    fill-empty-slots: true
    filler-item: "GRAY_STAINED_GLASS_PANE"
    items:
      info:
        slot: 10
        material: "BOOK"
        name: "&bTeam Info"
      members:
        slot: 12
        material: "PLAYER_HEAD"
        name: "&aMembers"
      home:
        slot: 13
        material: "RED_BED"
        name: "&cTeam Home"
      relations:
        slot: 14
        material: "IRON_SWORD"
        name: "&eAllies & Enemies"
      bank:
        slot: 16
        material: "GOLD_INGOT"
        name: "&6Team Bank"
      settings:
        slot: 22
        material: "COMPARATOR"
        name: "&7Team Settings"
        # Example: uncomment to give this item a custom resource-pack texture
        # custom-model-data: 1001

  members-menu:
    title: "&8&lTeam Members"
    size: 54
    show-online-status: true
    online-name-color: "&a"
    offline-name-color: "&7"

  invite-menu:
    title: "&8&lPending Invites"
    size: 27
    accept-material: "LIME_DYE"
    deny-material: "RED_DYE"

  relations-menu:
    title: "&8&lAllies"
    size: 45
    ally-material: "LIME_WOOL"

  confirm-menu:
    title: "&8&lAre you sure?"
    size: 27
    confirm-slot: 11
    deny-slot: 15
    confirm-material: "LIME_CONCRETE"
    deny-material: "RED_CONCRETE"

  team-list-menu:
    title: "&8&lAll Teams"
    size: 54
    teams-per-page: 45
    sort-by: "NAME" # LEVEL, MEMBERS, NAME

  settings-menu:
    title: "&8&lTeam Settings"
    size: 27
    pvp-toggle-slot: 13
    pvp-on-material: "LIME_DYE"
    pvp-off-material: "GRAY_DYE"
    pvp-locked-material: "BARRIER"
    # Cycles through team-customization.available-colors (left-click: next,
    # right-click: previous). Requires can-customize-team.
    color-slot: 11
    color-material: "WHITE_DYE"
    # An empty slot the player drops their desired icon item into, then
    # confirms with the apply button below. Requires can-customize-team.
    item-slot: 15
    item-placeholder-material: "ITEM_FRAME"
    item-apply-slot: 16
    item-apply-material: "LIME_CONCRETE"
    item-clear-slot: 20
    item-clear-material: "RED_CONCRETE"

  bank-menu:
    title: "&8&lTeam Bank"
    size: 27
    fill-empty-slots: true
    filler-item: "GRAY_STAINED_GLASS_PANE"
    balance-slot: 13
    balance-material: "GOLD_INGOT"
    deposit-slot: 11
    deposit-material: "LIME_DYE"
    withdraw-slot: 15
    withdraw-material: "RED_DYE"

messages:
  no-permission: "&cYou don't have permission to do that."
  no-team: "&cYou are not in a team."
  already-in-team: "&cYou are already in a team."
  team-created: "&aTeam &b{team} &acreated successfully!"
  team-disbanded: "&cYour team has been disbanded."
  invite-sent: "&aInvite sent to &b{player}&a."
  invite-received: "&bYou have been invited to join &e{team}&b."
  player-kicked: "&c{player} &7has been kicked from the team."
  not-enough-permission-role: "&cYour role does not allow this action."
  team-full: "&cThat team is full."
  invalid-name: "&cInvalid team name. Check length and allowed characters."
  cooldown-active: "&cYou must wait {time} before doing that again."
  pvp-enabled: "&aTeam PVP is now &lON&a."
  pvp-disabled: "&cTeam PVP is now &lOFF&c."
  pvp-locked: "&cTeam PVP toggle is locked because friendly-fire.within-team is enabled in the config."
  config-reloaded: "&aConfiguration reloaded."
  kicked-from-team: "&cYou have been kicked from &f{team}&c by &f{player}&c."
  player-kicked-broadcast: "&7{player} was kicked from the team by &f{kicker}&7."
  left-team: "&7You left &f{team}&7."
  player-left-broadcast: "&7{player} has left the team."
  player-joined-broadcast: "&a{player} has joined the team!"
  promoted-notify: "&aYou were promoted to &b{role}&a in &f{team}&a!"
  promoted-broadcast: "&a{player} was promoted to &b{role}&a."
  demoted-notify: "&cYou were demoted to &7{role}&c in &f{team}&c."
  demoted-broadcast: "&7{player} was demoted to {role}."
  ownership-transferred-notify: "&aYou are now the owner of &f{team}&a!"
  ownership-transferred-broadcast: "&e{player} is now the team owner."
  team-disbanded-member: "&cYour team &f{team} &chas been disbanded by the owner."

  # Bank
  bank-disabled: "&cThe team bank is disabled."
  bank-no-economy: "&cNo economy plugin (Vault) was found, so the team bank can't move real money right now."
  bank-balance: "&6Team Bank Balance: &f{amount}"
  bank-invalid-amount: "&cEnter a valid positive amount."
  bank-insufficient-player-funds: "&cYou don't have that much money."
  bank-insufficient-team-funds: "&cThe team bank doesn't have that much money."
  bank-max-balance: "&cThe team bank is already at its maximum balance."
  bank-transaction-failed: "&cThat transaction failed. Please try again."
  bank-cant-withdraw-role: "&cYour role isn't allowed to withdraw from the team bank."
  bank-deposit-success: "&aDeposited &f{amount} &ainto the team bank. New balance: &f{balance}&a."
  bank-withdraw-success: "&aWithdrew &f{amount} &afrom the team bank. New balance: &f{balance}&a."
  bank-deposit-broadcast: "&7{player} deposited &f{amount} &7into the team bank."
  bank-withdraw-broadcast: "&7{player} withdrew &f{amount} &7from the team bank."

  # Team customization (color & custom item)
  team-color-changed: "&aTeam color set to {color}."
  team-color-disabled: "&cTeam color customization is disabled."
  team-item-disabled: "&cTeam item customization is disabled."
  team-item-apply-empty: "&cPut an item in the slot first, then click apply."
  team-item-applied: "&aYour team's item has been updated! You got your item back."
  team-item-cleared: "&7Team item reset to default."
